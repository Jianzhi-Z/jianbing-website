<% 
const title = '';
const description = siteDescription;
%>

<%- include('partials/header') %>

<section class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <div class="absolute inset-0 overflow-hidden">
    <div class="absolute -top-1/2 -right-1/2 w-full h-full bg-gradient-to-b from-primary-500/10 to-transparent rounded-full blur-3xl"></div>
    <div class="absolute -bottom-1/2 -left-1/2 w-full h-full bg-gradient-to-t from-secondary-500/10 to-transparent rounded-full blur-3xl"></div>
  </div>
  
  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <div class="text-center lg:text-left" data-animate>
        <div class="inline-flex items-center space-x-2 px-4 py-2 rounded-full bg-primary-500/10 border border-primary-500/20 mb-6">
          <span class="w-2 h-2 rounded-full bg-primary-500 animate-pulse"></span>
          <span class="text-primary-400 text-sm font-medium">AI 探索者 · 持续创作中</span>
        </div>
        
        <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold mb-6 leading-tight">
          <span class="text-gradient">AI 行动家</span>
        </h1>
        
        <p class="text-xl md:text-2xl text-dark-text-secondary mb-8 leading-relaxed">
          探索人工智能的无限可能，用代码创造未来。<br>
          专注于AI智能体开发与编程技术分享。
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
          <a href="/articles" class="inline-flex items-center justify-center space-x-2 px-8 py-4 bg-primary-600 hover:bg-primary-700 text-white rounded-lg font-medium transition-all hover:shadow-lg hover:shadow-primary-500/25">
            <i data-lucide="book-open" class="w-5 h-5"></i>
            <span>阅读文章</span>
          </a>
          <a href="/products" class="inline-flex items-center justify-center space-x-2 px-8 py-4 bg-dark-surface hover:bg-dark-elevated border border-dark-border rounded-lg font-medium transition-all">
            <i data-lucide="box" class="w-5 h-5"></i>
            <span>查看产品</span>
          </a>
        </div>
        
        <div class="grid grid-cols-3 gap-8 mt-12 pt-12 border-t border-dark-border">
          <div>
            <div class="text-3xl font-bold text-primary-500"><%= articles.length %>+</div>
            <div class="text-dark-text-secondary text-sm mt-1">技术文章</div>
          </div>
          <div>
            <div class="text-3xl font-bold text-primary-500"><%= products.length %>+</div>
            <div class="text-dark-text-secondary text-sm mt-1">产品项目</div>
          </div>
          <div>
            <div class="text-3xl font-bold text-primary-500">1k+</div>
            <div class="text-dark-text-secondary text-sm mt-1">代码行数</div>
          </div>
        </div>
      </div>
      
      <div class="flex justify-center lg:justify-end" data-animate data-delay="2">
        <div class="relative">
          <div class="absolute inset-0 bg-gradient-to-r from-primary-500 to-secondary-500 rounded-full blur-2xl opacity-50 animate-pulse"></div>
          <img src="<%= profile && profile.avatar ? profile.avatar : '/images/avatar.jpg' %>" alt="煎饼" class="relative w-80 h-80 rounded-full object-cover border-4 border-dark-surface avatar-glow">
          
          <div class="absolute -top-4 -right-4 px-4 py-2 bg-dark-surface rounded-lg border border-dark-border shadow-xl">
            <div class="flex items-center space-x-2">
              <i data-lucide="code-2" class="w-4 h-4 text-primary-500"></i>
              <span class="text-sm font-medium">全栈开发</span>
            </div>
          </div>
          
          <div class="absolute -bottom-4 -left-4 px-4 py-2 bg-dark-surface rounded-lg border border-dark-border shadow-xl">
            <div class="flex items-center space-x-2">
              <i data-lucide="brain" class="w-4 h-4 text-secondary-500"></i>
              <span class="text-sm font-medium">AI 智能体</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
    <i data-lucide="chevron-down" class="w-6 h-6 text-dark-text-secondary"></i>
  </div>
</section>

<section class="py-20 bg-dark-surface/50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between mb-12" data-animate>
      <div>
        <h2 class="text-3xl md:text-4xl font-bold mb-2">最新文章</h2>
        <p class="text-dark-text-secondary">分享AI与编程领域的思考与实践</p>
      </div>
      <a href="/articles" class="hidden sm:inline-flex items-center space-x-2 text-primary-500 hover:text-primary-400 transition-colors">
        <span>查看全部</span>
        <i data-lucide="arrow-right" class="w-4 h-4"></i>
      </a>
    </div>
    
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      <% articles.forEach((article, index) => { %>
        <article class="card-hover bg-dark-surface rounded-xl overflow-hidden border border-dark-border" data-animate data-delay="<%= index + 1 %>">
          <a href="/articles/<%= article.slug %>" class="block">
            <div class="aspect-video overflow-hidden">
              <img src="<%= article.cover_image || 'https://picsum.photos/400/225?random=' + article.id %>" alt="<%= article.title %>" class="w-full h-full object-cover transition-transform hover:scale-105">
            </div>
            <div class="p-6">
              <div class="flex items-center space-x-2 mb-3">
                <span class="px-3 py-1 text-xs font-medium bg-primary-500/10 text-primary-400 rounded-full"><%= article.category %></span>
                <span class="text-dark-text-secondary text-sm"><%= formatDate(article.created_at) %></span>
              </div>
              <h3 class="text-xl font-semibold mb-2 line-clamp-2 hover:text-primary-500 transition-colors"><%= article.title %></h3>
              <p class="text-dark-text-secondary text-sm line-clamp-2"><%= article.excerpt || '暂无摘要' %></p>
            </div>
          </a>
        </article>
      <% }) %>
    </div>
    
    <div class="mt-8 text-center sm:hidden">
      <a href="/articles" class="inline-flex items-center space-x-2 text-primary-500 hover:text-primary-400 transition-colors">
        <span>查看全部文章</span>
        <i data-lucide="arrow-right" class="w-4 h-4"></i>
      </a>
    </div>
  </div>
</section>

<section class="py-20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16" data-animate>
      <h2 class="text-3xl md:text-4xl font-bold mb-4">我的产品</h2>
      <p class="text-dark-text-secondary max-w-2xl mx-auto">将AI技术转化为实用产品，解决真实世界的问题</p>
    </div>
    
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      <% products.forEach((product, index) => { %>
        <div class="card-hover bg-dark-surface rounded-xl p-6 border border-dark-border <%= product.featured ? 'md:col-span-2 lg:col-span-1 ring-2 ring-primary-500/20' : '' %>" data-animate data-delay="<%= index + 1 %>">
          <div class="flex items-start justify-between mb-4">
            <div class="w-12 h-12 rounded-lg bg-primary-500/10 flex items-center justify-center">
              <i data-lucide="box" class="w-6 h-6 text-primary-500"></i>
            </div>
            <% if (product.featured) { %>
              <span class="px-3 py-1 text-xs font-medium bg-secondary-500/10 text-secondary-500 rounded-full">Featured</span>
            <% } %>
          </div>
          <h3 class="text-xl font-semibold mb-2"><%= product.name %></h3>
          <p class="text-dark-text-secondary text-sm mb-4 line-clamp-2"><%= product.short_description || product.description %></p>
          <div class="flex flex-wrap gap-2 mb-4">
            <% if (product.tech_stack) { %>
              <% product.tech_stack.split(',').slice(0, 3).forEach(tech => { %>
                <span class="px-2 py-1 text-xs bg-dark-elevated rounded"><%= tech.trim() %></span>
              <% }) %>
            <% } %>
          </div>
          <a href="/products/<%= product.slug %>" class="inline-flex items-center space-x-2 text-primary-500 hover:text-primary-400 text-sm font-medium transition-colors">
            <span>了解更多</span>
            <i data-lucide="arrow-right" class="w-4 h-4"></i>
          </a>
        </div>
      <% }) %>
    </div>
  </div>
</section>

<section class="py-20">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="bg-gradient-to-r from-primary-600 to-primary-700 rounded-2xl p-8 md:p-12 text-center" data-animate>
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">有项目想法？</h2>
      <p class="text-primary-100 text-lg mb-8 max-w-xl mx-auto">无论是AI智能体开发还是编程咨询，我都很乐意与您讨论合作机会。</p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/about" class="inline-flex items-center justify-center space-x-2 px-8 py-4 bg-white text-primary-600 rounded-lg font-medium hover:bg-primary-50 transition-colors">
          <i data-lucide="user" class="w-5 h-5"></i>
          <span>了解更多</span>
        </a>
        <a href="mailto:hello@jianbing.dev" class="inline-flex items-center justify-center space-x-2 px-8 py-4 bg-primary-700 text-white border border-primary-500 rounded-lg font-medium hover:bg-primary-800 transition-colors">
          <i data-lucide="mail" class="w-5 h-5"></i>
          <span>联系我</span>
        </a>
      </div>
    </div>
  </div>
</section>

<%- include('partials/footer') %>
